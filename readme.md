# Currency Quotes Service

Асинхронный HTTP-сервис для обновления и получения валютных котировок

## tl;dr

Асинхронный сервис котировок валют на Go.

- Сделал на основе **Clean / Hexagonal Architecture**
- Задачи воркера идут через каналы
- Реализована exactly-once методика
- Старался по минимум пакетов использовать, чтобы показать навыки чистой гошки. По той же причине отказался от пакетов для работы с секретами и написал свое решение на основе reflect'а

## Схема процесса

[Посмотреть диаграмму в Excalidraw](https://excalidraw.com/#json=UJMDxj6Xawh81zWumU9t5,28mrdYke_EpqP8_EHSK_Fg)

---

## Запуск
1. Вызовите генерацию документации через `makeFile` или `консоль`
2. Вставьте свой ключ от **api.exchangeratesapi.io** в `.env` файл
3. Запустите `docker-compose up`
4. Дождитесь запуска нодов
5. Переходите на `http://localhost:8080/swagger/index.html`
6. Happy review!

---

## 📌 Основные возможности

- Асинхронное обновление валютных котировок
- Получение котировки по идентификатору обновления
- Получение последнего актуального значения котировки по валюте
- Идемпотентная обработка обновлений
- Хранение данных в PostgreSQL
- Юнит-тесты бизнес-логики (использовал Table drive)

---

## 🏗 Архитектура

Проект построен с использованием принципов Clean Architecture:


### Ключевые принципы:
- Domain слой не зависит от инфраструктуры
- Application слой инкапсулирует бизнес-логику
- Адаптеры отвечают за ввод/вывод (HTTP, фоновые задачи)
- Все зависимости направлены внутрь

---

## 🔄 Асинхронная модель

Обновление котировки происходит в фоновом режиме:

1. Клиент отправляет запрос на обновление валюты
2. Сервис создаёт job и возвращает `update_id`
3. Обновление выполняется асинхронно воркером
4. Клиент может запросить:
    - результат по `update_id`
    - последнее актуальное значение по валюте

HTTP-хендлер **не выполняет обновление напрямую**.

---

## 🔐 Идемпотентность и Exactly-Once Processing

- Каждое обновление имеет уникальный `job_id`
- Повторная обработка одного и того же job безопасна
- Все изменения состояния выполняются в транзакциях
- Используются идемпотентные SQL-операции (`ON CONFLICT`)
- Применяется модель `at-least-once delivery + idempotent writes`

Бизнес-состояние изменяется **ровно один раз**, даже при повторных вызовах.

---

## 🌍 Внешний источник данных

Для получения курсов используется внешний API валютных котировок  
(например, exchangeratesapi.io).

Источник данных инкапсулирован и легко заменяется.

---

## 🛢 Хранилище данных

- PostgreSQL
- SQL-миграции
- Отдельные таблицы для:
    - job-ов обновления
    - котировок валют

---

## 🚀 API

После запуска проекта перейдите на `/swagger/index.html`

---

## 🧪 Тестирование

- Покрыта ключевая бизнес-логика (use cases)
- Тесты изолированы от инфраструктуры
- Используются mock-репозитории (mockery)